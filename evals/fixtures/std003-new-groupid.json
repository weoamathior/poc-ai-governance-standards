{
  "name": "STD-003 new groupId (second HTTP client)",
  "intent": "should-flag",
  "diff": [
    "diff --git a/pom.xml b/pom.xml",
    "@@ -40,6 +40,13 @@",
    "         </dependency>",
    " ",
    "+        <!-- Second HTTP client added alongside spring-web -->",
    "+        <dependency>",
    "+            <groupId>com.squareup.okhttp3</groupId>",
    "+            <artifactId>okhttp</artifactId>",
    "+            <version>4.12.0</version>",
    "+        </dependency>",
    "+",
    "         <dependency>",
    "             <groupId>org.springframework.boot</groupId>"
  ],
  "expect": [
    {
      "standardId": "STD-003",
      "severity": "escalate"
    }
  ],
  "expectNot": [],
  "runs": 5,
  "passThreshold": 3,
  "tolerance": 0,
  "stubFindings": {
    "findings": [
      {
        "standardId": "STD-003",
        "severity": "escalate",
        "description": "New groupId com.squareup.okhttp3 (okhttp) duplicates existing HTTP-client capability.",
        "file": "pom.xml",
        "lineRange": null
      }
    ]
  }
}
